<UserControl x:Class="TallyAuditAssistant.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="0,0,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Audit Working Papers &amp; Reports Center" FontSize="22" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Generate, compile, and export professional statutory audit summaries, tax reconciliations, and structured spreadsheets." Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,12"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="2" VerticalAlignment="Top" Margin="0,10,0,0">
            <!-- Excel Export Card -->
            <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="📊 Microsoft Excel Working Papers" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                    <TextBlock Text="Generates a complete XML spreadsheet package containing separated worksheets for GST findings, TDS exceptions, ledgers, and duplicate detection check logs." Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,20" TextWrapping="Wrap" Height="60"/>
                    <Button Content="📥 Export Excel Working Papers" Command="{Binding GenerateExcelReportCommand}" HorizontalAlignment="Stretch" Padding="14,10"/>
                </StackPanel>
            </Border>

            <!-- HTML/PDF Summary Card -->
            <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="📄 Executive HTML Summary Report" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                    <TextBlock Text="Produces a clean, highly-formatted static HTML summary page suitable for converting to PDF, reporting key metrics, company metadata, and critical findings." Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,20" TextWrapping="Wrap" Height="60"/>
                    <Button Content="📥 Generate Summary Report" Command="{Binding GeneratePdfReportCommand}" HorizontalAlignment="Stretch" Padding="14,10"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
