<UserControl x:Class="TallyAuditAssistant.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <TextBlock Text="System &amp; Audit Settings" FontSize="22" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Manage local SQLite storage, Tally communication defaults, and mock test modes." Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,20"/>

            <!-- Storage Settings -->
            <Border Padding="16" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Local Storage (Offline SQLite Engine)" FontWeight="Bold" FontSize="15" Foreground="White" Margin="0,0,0,8"/>
                    <TextBlock Text="Database File Path:" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding DatabasePath, Mode=OneWay}" IsReadOnly="True" Padding="8" Background="#0F172A" Foreground="#94A3B8" BorderBrush="{StaticResource BorderBrush}"/>
                    <TextBlock Text="All accounting vouchers, masters, and audit evidence remain exclusively on this computer." FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- Tally Defaults -->
            <Border Padding="16" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Tally Default Host &amp; Port" FontWeight="Bold" FontSize="15" Foreground="White" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="140" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Host Address" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding TallyHost, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="#0F172A" Foreground="White" BorderBrush="{StaticResource BorderBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Port" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding TallyPort, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="#0F172A" Foreground="White" BorderBrush="{StaticResource BorderBrush}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Test / Mock Mode -->
            <Border Padding="16" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Developer &amp; Testing Mode" FontWeight="Bold" FontSize="15" Foreground="White" Margin="0,0,0,8"/>
                    <CheckBox IsChecked="{Binding IsMockMode}" Margin="0,4,0,0">
                        <TextBlock Text="Enable Mock Tally Integration (Simulates Tally responses without live TallyPrime instance)" Foreground="White" FontSize="13"/>
                    </CheckBox>
                </StackPanel>
            </Border>

            <!-- Save & Status -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="💾 Save Settings" Command="{Binding SaveSettingsCommand}" Padding="20,10"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#10B981" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
