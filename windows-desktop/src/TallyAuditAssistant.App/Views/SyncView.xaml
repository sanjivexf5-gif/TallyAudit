<UserControl x:Class="TallyAuditAssistant.App.Views.SyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1050">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="TallyPrime Data Synchronization Engine" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Transaction-safe local SQLite synchronization with AlterId delta tracking and streaming batching." Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="⚡ Full Sync" Command="{Binding StartFullSyncCommand}" IsEnabled="{Binding IsSyncing, Converter={x:Null}}" Margin="0,0,8,0" Padding="16,8"/>
                    <Button Content="🔄 Incremental Sync" Command="{Binding StartIncrementalSyncCommand}" IsEnabled="{Binding IsSyncing, Converter={x:Null}}" Background="#0E7490" Margin="0,0,8,0" Padding="16,8"/>
                    <Button Content="⏸ Pause" Command="{Binding PauseSyncCommand}" IsEnabled="{Binding IsSyncing}" Background="#D97706" Margin="0,0,8,0" Padding="12,8"/>
                    <Button Content="▶ Resume" Command="{Binding ResumeSyncCommand}" IsEnabled="{Binding IsPaused}" Background="#059669" Margin="0,0,8,0" Padding="12,8"/>
                    <Button Content="⏹ Cancel" Command="{Binding CancelSyncCommand}" IsEnabled="{Binding IsSyncing}" Background="#DC2626" Margin="0,0,8,0" Padding="12,8"/>
                    <Button Content="🔁 Retry" Command="{Binding RetrySyncCommand}" IsEnabled="{Binding IsSyncing, Converter={x:Null}}" Background="#475569" Padding="12,8"/>
                </StackPanel>
            </Grid>

            <!-- 8-Stage Pipeline Visual Ribbon -->
            <Border Padding="14" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="PIPELINE STAGE EXECUTION" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="CONNECT → SELECT COMPANY → READ MASTERS → READ TRANSACTIONS → VALIDATE → STORE → INDEX → COMPLETE" 
                               FontFamily="Consolas, Courier New" FontSize="12" FontWeight="Bold" Foreground="#38BDF8" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding CurrentTaskDescription}" FontSize="12" FontWeight="SemiBold" Foreground="#10B981" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- Progress Bar -->
            <Border Padding="14" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <TextBlock Text="{Binding CurrentStageText, StringFormat={}Stage: {0}}" FontWeight="Bold" Foreground="White" FontSize="13"/>
                        <TextBlock Text="{Binding ProgressPercentage, StringFormat={}{0:F1}%}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#10B981" FontSize="13"/>
                    </Grid>
                    <ProgressBar Grid.Row="1" Height="10" Value="{Binding ProgressPercentage}" Maximum="100" Background="#0F172A" Foreground="{StaticResource PrimaryBrandBrush}"/>
                </Grid>
            </Border>

            <!-- Real-time Synchronization Metric Counters -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="DISCOVERED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding RecordsDiscovered}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="PROCESSED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding RecordsProcessed}" FontSize="16" FontWeight="Bold" Foreground="#38BDF8" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="INSERTED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding RecordsInserted}" FontSize="16" FontWeight="Bold" Foreground="#10B981" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="UPDATED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding RecordsUpdated}" FontSize="16" FontWeight="Bold" Foreground="#F59E0B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="SKIPPED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding RecordsSkipped}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="5" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="ERRORS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding Errors}" FontSize="16" FontWeight="Bold" Foreground="#EF4444" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="6" Margin="2,0,4,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="SPEED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding ItemsPerSecond, StringFormat={}{0} /s}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="7" Margin="2,0,0,0" Padding="10" Background="{StaticResource CardBackgroundBrush}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="ELAPSED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding ElapsedTimeText}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Live Streaming Log Console -->
            <Border Padding="14" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="LIVE TRANSACTION STREAM LOG" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding LiveLogs}" Height="140" Background="#070B14" BorderThickness="0" FontFamily="Consolas, Courier New" FontSize="11" Foreground="#38BDF8"/>
                </StackPanel>
            </Border>

            <!-- Sync History Table -->
            <Border Padding="14" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="HISTORICAL AUDIT SYNCHRONIZATIONS" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                    <DataGrid ItemsSource="{Binding SyncHistory}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat={}{0:dd-MMM-yyyy HH:mm}}" Width="150"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding SyncType}" Width="100"/>
                            <DataGridTextColumn Header="Vouchers" Binding="{Binding VouchersFetched, StringFormat={}{0:N0}}" Width="110"/>
                            <DataGridTextColumn Header="Masters" Binding="{Binding MastersFetched, StringFormat={}{0:N0}}" Width="100"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding DurationMs, StringFormat={}{0} ms}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Message / Error" Binding="{Binding ErrorMessage}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
